<UserControl x:Class="MyGarden2.UserControls.GardenBeds"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyGarden2.UserControls"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="667" d:DesignWidth="873">
    <Grid>
        <Grid Width ="250" HorizontalAlignment="Left" Background="#FF8ED911" Opacity="0.8">
            <StackPanel>
                <ListView Name="GardenBedsListView"
                          ItemsSource="{Binding GardenBeds}"
                          SelectedItem="{Binding SelectedGardenBed, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Text}" FontSize="20" FontWeight="Bold" Margin="15"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Button x:Name="AddButton" Content="Добавить новую грядку" Width="100" Margin="5" Click="AddButton_Click" Background="DarkGreen"/>
            </StackPanel>
        </Grid>
        <Grid Margin="250 0 0 0">
            <ContentControl Content="{Binding CurrentView}" />
        </Grid>
    </Grid>
</UserControl>
