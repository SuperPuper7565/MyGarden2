<UserControl x:Class="MyGarden2.UserControls.DatePickerPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="160" d:DesignWidth="280">
    <UserControl.Resources>
        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="YellowGreen"/>
            <Setter Property="Foreground" Value="DarkGreen"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="DarkGreen"/>
        </Style>

        <!-- Улучшенный стиль для текстового поля DatePicker -->
        <Style TargetType="DatePickerTextBox">
            <Setter Property="Background" Value="YellowGreen"/>
            <Setter Property="Foreground" Value="DarkGreen"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <Grid>
                            <Border x:Name="Border" 
                                    Background="{TemplateBinding Background}" 
                                    BorderThickness="0"
                                    BorderBrush="Transparent"
                                    CornerRadius="0"/>
                            <TextBox x:Name="PART_TextBox"
                                     Text="{Binding Path=SelectedDate, StringFormat='dd.MM.yyyy', RelativeSource={RelativeSource AncestorType={x:Type DatePicker}}}"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{TemplateBinding Foreground}"
                                     Padding="{TemplateBinding Padding}"
                                     HorizontalContentAlignment="Stretch"
                                     VerticalContentAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Основной контейнер с тенями и границами -->
        <Border Background="YellowGreen" 
                BorderBrush="DarkGreen" 
                BorderThickness="5" 
                Padding="15"
                SnapsToDevicePixels="True"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Width="280">

            <Border.Effect>
                <DropShadowEffect Color="DarkGreen" 
                                 Direction="320" 
                                 ShadowDepth="5" 
                                 Opacity="0.5"
                                 RenderingBias="Quality"/>
            </Border.Effect>

            <!-- Основной стек панели -->
            <StackPanel>
                <!-- Заголовок -->
                <TextBlock Text="Выберите дату посадки" 
                           FontSize="18" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,15"
                           Foreground="DarkGreen"
                           TextAlignment="Center"/>

                <!-- Поле выбора даты -->
                <DatePicker x:Name="DateSowingPicker"
                            SelectedDate="{Binding DateSowing, Mode=TwoWay, StringFormat='dd.MM.yyyy'}"
                            FirstDayOfWeek="Monday"
                            FontSize="14"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Width="220"
                            Margin="0,0,0,20"
                            Background="YellowGreen"
                            Foreground="DarkGreen">

                    <!-- Стили для выпадающего календаря -->
                    <DatePicker.Resources>
                        <Style TargetType="Calendar">
                            <Setter Property="Background" Value="YellowGreen"/>
                            <Setter Property="BorderBrush" Value="DarkGreen"/>
                        </Style>
                        <Style TargetType="CalendarDayButton">
                            <Setter Property="Background" Value="YellowGreen"/>
                            <Setter Property="Foreground" Value="DarkGreen"/>
                            <Setter Property="BorderBrush" Value="DarkGreen"/>
                        </Style>
                    </DatePicker.Resources>
                </DatePicker>

                <!-- Кнопки -->
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom">

                    <Button Content="Подтвердить" 
                            MinWidth="100"
                            Margin="0,0,10,0" 
                            Click="ConfirmButton_Click" 
                            Background="DarkGreen"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Padding="10,5"/>

                    <Button Content="Отмена" 
                            MinWidth="100"
                            Margin="10,0,0,0" 
                            Click="CancelButton_Click" 
                            Background="DarkGreen"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Padding="10,5"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>