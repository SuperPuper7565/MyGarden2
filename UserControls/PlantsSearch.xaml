<UserControl x:Class="MyGarden2.UserControls.PlantsSearch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyGarden2.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="667" d:DesignWidth="250">
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Height="40">
            <TextBlock HorizontalAlignment="Left" Text="Поиск:" FontFamily="Arial" FontSize="24"/>
            <TextBox Name="searchBox" Width="150" Margin="5" HorizontalAlignment="Right" FontFamily="Arial" FontSize="16" TextChanged="SearchBox_TextChanged"/>
        </StackPanel>
        <ListView Name="plantsListView" 
                  ItemsSource="{Binding Plants}" 
                  Margin="0 40 0 0"
                  SelectionChanged="PlantsListView_SelectionChanged"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="Black" Padding="10" Margin="5">
                        <Grid Width="220">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="64"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding ImagePath}" Width="64" Height="64" Stretch="Uniform" Grid.Column="0"/>
                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Margin="15" Grid.Column="1"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
